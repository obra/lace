# Credential capability boundary (#6) Implementation Plan — rev 3

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:test-driven-development + superpowers:executing-plans (or subagent-driven-development). Steps use checkbox (`- [ ]`). **Two repos:** the bulk is **sen-core-v2** (helper, per-container sockets, the Rust shim/plane provisioning, proxy/token cleanup); **lace** (TypeScript, `packages/agent`) does a focused **decoupling** (delete the token minter + the `browserCdpSocket` carrier + generalize hardcoded mount names). **Chase by symbol, not line number** — the lace tree is mid-merge; #3 landed; lines have shifted.

> **rev 3 (2026-06-04):** a confirmatory 3-opus panel on rev 2 found TWO architectural blockers + that rev-2's decoupling claim was false. Corrections (all verified in code):
> - **BLOCKER — the per-container socket was reachable by main-sen.** rev 2 put the socket under the instance root (`/var/sen/instance/...`), which main-sen + snapshotter bind-mount **RW** (`docker-compose.yml`); `mounts.rs guard_path` even *requires* paths under the instance root unless flagged `sharedPath`. Since lace agents are processes in main-sen's shared container, any sibling (e.g. one with a file tool) could `connect()` to *any* persona's socket directly and **pick its identity** — a worse forgery primitive than the token. ⇒ **Per-container sockets move to the broker-private volume `sen-credential-private`** (already used to keep admin/approval sockets off the instance root, invisible to main-sen), via a new `sharedPath` registry entry; the shim mounts the **single socket file** (not its dir) into the persona.
> - **BLOCKER — "credential tool = sen plugin" was incoherent.** Plugin `Tool`s run **in-process** in the lace node process and reach containers only via `ctx.runtime` (fs/exec/network); there is NO "plugin tool runs inside the persona container and opens `/run/sen-cred.sock`" primitive. The real credential tool is `/usr/local/bin/sen-request-credential`, a **CLI baked into the persona images** (`docker/persona-containers/*/Dockerfile`, source `src/cli/request-credential.ts`), invoked by the agent via bash *inside* the container — already not-a-lace-thing. ⇒ **The credential tool STAYS an in-image CLI** (Jesse); drop the plugin + lace-manifest framing entirely. The capability gate is **the mount** — a persona that declares the `sen-cred` mount gets a socket; the **plane enforces it** (registry-truth).
> - **The decoupling claim was FALSE; #6 now does the FULL decoupling** (Jesse). lace's *generic* kernel still carries the real coupling: the **`browserCdpSocket`/`SEN_BROWSER_CDP_SOCKET` carrier** baked into the generic runtime descriptor + projection (~6 files), the `tokenEnvName`/`tokenFingerprint` metadata plumbing, and hardcoded `sen-cred`/`sen-ca`/`sen-browser-cdp` mount names. Part B now deletes all of it so lace ends **genuinely** credential-agnostic (task #18's actual debt).
> - **Feasibility fixes:** the shim computes the socket path (the `{child_session}` placeholder is a red herring — placeholders are substituted shim-side from registry mount *names*, never sent to the helper; and **persistent-box has no child session** — it's the credential-capable persona). `cli/helper-entrypoint.ts` **does exist** (rev-2's "invented file" was an over-correction — the shell `entrypoint.sh` execs it); the residue to delete is the in-process *gates*. Rust trait is **`TokenMinter`** not `TokenSource`. **spawn-broker is ALIVE/selectable** (`manager-factory.ts` precedence on `SEN_SPAWN_BROKER_SOCKET`) — its minter survives unless deleted. The `browser_cdp_url` deletion list was incomplete (+`lace-runtime-registration.ts`, `remote-runtime-registrar.ts`, `types.ts`, `chromium-cdp-transport.ts`). Restart-rehydration rec (b plane-reprovision) is infeasible (the shim is stateless/reactive) → persist or document.
> - **Confirmed solid (panel-verified):** token-minter enumeration; the #4-unbuilt HARD BLOCK; the cutover order; the fingerprint label is safe to delete (ownership keys on `sen.broker.persona`); the lace manifest gate is dead code (so dropping it loses nothing); PRI-2002 CDP is already loopback-only (`:9222` has no EXPOSE/publish).

> **Sequencing — #6 is LAST: #3 → #4 → #6.** #3 (the plane) IS merged. **#4 (proxy source-IP attribution + token-free proxy) is NOT** — Part C is blocked on it.

**Goal:** Delete `SEN_AGENT_TOKEN` end-to-end and make lace **genuinely credential-agnostic**. Replace the world-writable, token-authed credential socket with a **per-persona-container socket on the broker-private volume, plane-provisioned and bound to registry-truth identity** — identity is *which socket your container has*, not a stealable secret, and not reachable from main-sen. The credential tool stays an **in-image CLI**; the gate is the persona's declared `sen-cred` mount. The helper runs in its **own container** (vault memory unreadable by main-sen). Browser CDP stays **loopback-only** (already true via PRI-2002); kill any network form. lace loses the token minter, the `browserCdpSocket` carrier, and hardcoded credential mount names.

**Architecture:** Authentication is **structural** — credentials are reachable only from inside a persona container whose socket the **trusted plane** created (on a volume main-sen can't see) and bound to the persona's **registry-truth** identity. No bearer secret. lace ships nothing credential-specific; sen owns the in-image CLI, the helper, and the protocol.

---

## Repo state / posture / trust
- **lace** (TS): `/home/jesse/git/prime-radiant/sen2/lace`, `packages/agent/src`. **Mid-merge** (`pri2012-shim-lace`→`main`; some `containers/`+`tools/runtime/` files have conflict markers) — confirm post-merge state; chase by symbol.
- **sen-core-v2**: `/home/jesse/git/prime-radiant/sen2/sen-core-v2` — `src/credential-injection/` (helper, browser, registry), `src/cli/request-credential.ts` (the in-image CLI source), `sen-docker/` (Rust shim/plane), `proxy/` + `docker/credential-proxy/` + `docker/credential-helper/` + `docker/persona-containers/`.
- Posture: **not in production, Ada is the only runner, we own the box, code is trusted as code.** Fix-by-rebuild; **no backward-compat for removed pre-v1 surfaces** (token, in-process helper gates, network `browser_cdp_url`, the shared 0666 socket, spawn-broker — DELETED). Box-coordinated; lands under `--recreate`.
- **Trust split:** the **plane (shim)** is trusted (holds docker.sock = total container control) and is the **sole identity-asserter**. **main-sen is semi-trusted** — orchestrates spawns but must never hold secrets, never assert credential identity, and (now) never be able to reach a credential socket. The **broker-private volume** is the main-sen-invisible substrate that makes that real.

## The boundary model (read first)
- **Today (verified):** the credential socket is a single `0o666` socket (`helper-server.ts:220`) under the instance root, mounted dir→`/run` into persona containers (`src/main.ts:~1203`), authenticated **solely** by `SEN_AGENT_TOKEN` — minted in lace (`job-manager.ts:218`), the **Rust shim** (`dispatch.rs:167` `TokenMinter::mint` → `spawn.rs:139` env), a **synthetic** mint in the helper (`runtime-registry.ts:114`), and the **spawn-broker** (`spawn-broker-identity.ts`). Identity = `registry.resolve({token})` keyed on `tokenFingerprint`. Helper defaults to **in-process in main-sen**. No peer-cred. The credential *tool* is the in-image CLI `sen-request-credential` (`src/cli/request-credential.ts`), invoked via bash in the persona container.
- **After #6:** credentials are reachable **only from inside a persona container**. The **plane provisions one credential socket per persona container on the broker-private volume**, binds it to that persona's **registry-truth `(persona, session, job)`**, and bind-mounts the **single socket file** at `/run/sen-cred.sock`. **Identity = which socket.** No token. Helper in its **own container**. Browser CDP loopback-only via the existing unix-socket relay. lace carries no credential-specific code.
- **Why it's sound now:** (1) the socket is on a volume **main-sen cannot see** → the shared-container lace processes can't reach any persona's socket (the rev-2 hole, closed); (2) each persona container has only its own single socket file mounted → no cross-container reach; (3) identity is bound by the **plane** (registry-truth), never caller-asserted → no forgery; (4) no token to steal; (5) vault memory isolated in the helper container.
- **Threats closed:** token theft; cross-agent credential read; identity forgery (incl. via main-sen reaching the socket); main-sen reading vault memory; lace knowing a sen secret.

## Decisions (Jesse, 2026-06-04)
1. Identity = **per-persona-container socket on the broker-private volume, plane-provisioned, bound to registry-truth identity**. Credentials used **only** inside persona containers (the in-image CLI). **No SO_PEERCRED, no dedicated uid** (useless under shared uid 1000; the separate helper container is the vault-memory boundary).
2. #6 **builds on #4**; #3→#4→#6; the token dies completely across **all** minters (incl. spawn-broker).
3. The credential tool **stays an in-image CLI**; the gate is the persona's declared `sen-cred` mount (plane-enforced). **Drop the plugin + lace-manifest framing.**
4. #6 does the **FULL lace decoupling**: delete the token minter + the `browserCdpSocket` carrier + generalize hardcoded mount names → lace genuinely credential-agnostic.
5. CDP/D1 = **verify the existing PRI-2002 unix-socket loopback relay is the only path + delete any network `browser_cdp_url`** (complete site list). No plane-exec rewrite.

---

## Part A — Per-persona-container credential socket on the broker-private volume, plane-provisioned

**Files (sen-core-v2):** `sen-docker/src/{dispatch.rs, helper.rs, mounts.rs}` + `sen-docker/deploy/etc/sen-mount-registry.json` (the shim: provision + single-file mount + register); `src/credential-injection/{helper-server.ts, helper-lifecycle.ts, runtime-registry.ts, socket-paths.ts, proxy-helper-protocol.ts}`; `docker-compose.yml` (private-volume wiring).

- [ ] **Step 1: helper container is the only mode.** Delete the in-process helper *gates* (`isCredentialHelperEnabled` `helper-lifecycle.ts:354`, `isCredentialBrokerActive` `:377`, in-process wiring). The dedicated helper container + its `sen` (uid 1000, gosu) `docker/credential-helper/entrypoint.sh` → `dist/credential-injection/cli/helper-entrypoint.js` (source `src/credential-injection/cli/helper-entrypoint.ts` — **both exist; do NOT delete the TS entrypoint**) already work — keep them. Do **not** claim a distinct uid (uid 1000 is deliberate). Test: in-process path gone (grep gate).
- [ ] **Step 2: per-container socket on the broker-private volume.** Add a registry entry for a per-container credential socket whose source is on **`sen-credential-private`** (the volume already mounted into the helper/proxy/arbiter but **NOT** main-sen — `docker-compose.yml`), flagged **`sharedPath: true`** so `mounts.rs guard_path` (`mounts.rs:112-134`) allows it outside the instance root. The **shim computes** the per-container socket host path (keyed on container name or `(persona,session,job)` — symmetric with how it already pre-creates `/work` scratch at `dispatch.rs:~154`; **do NOT use the `{child_session}` placeholder** — it isn't sent to the helper and is undefined for the child-less persistent-box). Test: the resolved socket path is on the private volume, never under the instance root; persistent-box (no child session) resolves a valid path.
- [ ] **Step 3: provision handshake (replaces the token mint).** The shim, at spawn, for a persona that **declares the `sen-cred` mount** (the gate — see Part B Step 4):
  - calls a new helper **admin** op **`provision_capability_socket`** (over the existing shim→helper admin channel — `helper.rs:38-69`, dispatched at `helper-server.ts:~693`, beside `adminRegisterRuntime` `:1231`; the shim already calls `register_runtime` before `build_create_argv`, `dispatch.rs:185`→`:197`, so the round-trip fits) passing **the shim-computed socket path** + the **registry-truth-derived** `{persona, session, job}`. (Persona is a caller-selected **closed 3-value enum** `validate.rs:9`; everything bound is derived from the vetted persona spec, so a caller can't assert an arbitrary identity.)
  - the helper `bind()+listen()`s on **exactly that path**, **binds socket-path → identity** (replacing the `tokenFingerprint→identity` map in `runtime-registry.ts:73/107`), returns success. Socket perms connectable by uid 1000 inside the persona (not `0o666`; delete the `helper-server.ts:220` chmod).
  - the shim bind-mounts the **single socket file** at `/run/sen-cred.sock` (single file, NOT the private-volume dir — never expose admin/approval sockets to a persona).
  - on teardown the shim calls **`release_capability_socket`** (close + unlink + drop the map entry) — see Cross-cutting reuse-safety.
  - **Identity dispatch = which socket**: `handleLine` (`helper-server.ts:~350`) resolves identity from the accepting socket's association, NOT `resolve({token})`. Subagent ops unchanged. Delete `resolve_source_ip` (`:433`) + the synthetic mint (`runtime-registry.ts:114`) — coordinate the proxy half with #4 (Part C).
  - Test: a persona container has only its own `/run/sen-cred.sock`; two personas → different host sockets → different identities; the helper resolves identity purely from the socket; a connection on A's socket never resolves to B; **main-sen cannot reach any persona's socket** (the private-volume isolation — assert the socket dir is absent from main-sen's mounts).
- [ ] **Step 4: no caller-asserted identity.** The CLI/agent never sends `persona`/`session`/`job`; the helper derives them from the socket binding. Remove any subagent-wire field that lets the caller assert identity. Test: a request with a `persona` field is ignored.

## Part B — Full lace decoupling (credential tool stays an in-image CLI)

**Files (lace):** `jobs/job-manager.ts` + `core/server-types.ts` + `core/container-execution-metadata.ts` + `jobs/subagent-job.ts` (token metadata); `tools/runtime/{types.ts,validation.ts}` + `tools/runtime/projected-container.ts` + `jobs/persona-container-spec.ts` + `containers/spec.ts` + `containers/broker-container-mounts.ts` (the `browserCdpSocket` carrier + mount names). **Chase ALL by symbol.**

- [ ] **Step 1: delete lace's token minter + metadata plumbing.** `buildContainerExecutionContext` (`job-manager.ts:205`): remove the `randomBytes(32)` token (`:218`), `executionEnv:{[tokenEnvName]:token}` (`:225`), `tokenFingerprint` (`:228`), the `mergeEmbedderSpawnEnv` `SEN_AGENT_TOKEN` special-case (`:171/196-199`). Then remove the now-dead **metadata types**: `ContainerExecutionIdentityConfig.tokenEnvName`/`tokenFingerprint` (`server-types.ts:~68`), `container-execution-metadata.ts`, and the `subagent-job.ts:~372` consumers. Test: no lace file references the token env name or `tokenFingerprint` (grep gate); a spawned container has no `SEN_AGENT_TOKEN`.
- [ ] **Step 2: strip the `browserCdpSocket` carrier from lace's GENERIC layer.** `browserCdpSocket`/`SEN_BROWSER_CDP_SOCKET` is sen-credential-specific but sits in lace's generic runtime descriptor + projection: `tools/runtime/types.ts:~73`, `validation.ts:~89`, `projected-container.ts:~264`, `persona-container-spec.ts:~90`, `containers/spec.ts:~17`. **Remove the special field**; the CDP socket becomes **just another embedder-declared mount** (the persona declares it in its mounts like `sen-cred`). lace stops knowing "browser CDP." Test: no `browserCdpSocket`/`SEN_BROWSER_CDP_SOCKET` symbol in lace (grep gate); the browser persona still gets its CDP socket via a generic mount.
- [ ] **Step 3: generalize the hardcoded credential mount names.** `containers/broker-container-mounts.ts:~62` hardcodes `sen-cred`/`sen-ca`/`sen-browser-cdp`. These are embedder concepts — make them **embedder-supplied** (the persona/registry declares them), not lace literals. Test: lace has no `sen-cred`/`sen-ca`/`sen-browser-cdp` string literals (grep gate).
- [ ] **Step 4: the gate is the persona's `sen-cred` mount (plane-enforced); drop the lace manifest path.** A persona's credential-ness = it **declares the `sen-cred` mount** (resolved at `mounts.rs:47`, `dispatch.rs:103 load_persona`). The plane provisions a socket (Part A) **only** for such personas — that IS the gate (registry-truth, plane-side). `PersonaSpec` (`sen-docker/src/persona.rs`, `deny_unknown_fields`) has no capability field and **needs none** (the mount declaration suffices); if a more explicit declaration is wanted later, add a field — YAGNI for v1. **Drop** the lace `pluginMayUseCapability` enforcement from #6 — it's dead code (`manifest.ts:17`) and not the boundary; the credential tool is an in-image CLI, not a plugin. The in-image CLI (`src/cli/request-credential.ts`) is **unchanged** except it stops sending a token (identity is the socket). Test: a persona without the `sen-cred` mount gets no socket and the CLI fails closed.

## Part C — End-to-end `SEN_AGENT_TOKEN` death (BLOCKED on #4)

> **HARD BLOCK — verify in code before starting Part C; ALL are currently RED:** (a) the proxy resolves identity by source-IP with **no** `resolve_source_ip`/synthetic token in `transparent.rs`; (b) the empty-identity fallback at `transparent.rs:~406-420` is **deleted** (deny); (c) `spawn.rs` no longer injects `SEN_AGENT_TOKEN` (`:139`); (d) per-agent networks live (no shared `--network quarantine` `:119`). These are **#4's** deliverables and are **all unbuilt today**. If any is missing, **STOP** — deleting the token now bricks egress credential injection.

- [ ] **Step 1: delete ALL minters** (verified enumeration):
  - lace `job-manager.ts:218` — done in Part B Step 1.
  - **Rust shim (LIVE path):** `dispatch.rs:167` `self.minter.mint()` + the **`TokenMinter`** trait (`dispatch.rs:~22`); `spawn.rs:139` env, `SpawnIdentity.token`/`token_fingerprint` (`:22-23`), and the `sen.broker.token-fingerprint` label (`LABEL_FINGERPRINT` `:14`, push `:135` — **safe to delete**; ownership/reaping key on `sen.broker.persona` only, `ownership.rs`/`dispatch.rs:393`).
  - **Helper synthetic mint:** `runtime-registry.ts:114`.
  - **spawn-broker (ALIVE, not dead):** `manager-factory.ts:~84` selects `SpawnBrokerContainerRuntime` (precedence) whenever `SEN_SPAWN_BROKER_SOCKET` is set; `spawn-broker-server.ts:~219 mintToken()` + `spawn-broker-identity.ts`. Per the kit it's superseded by the shim — **delete the whole `containers/spawn-broker-*.ts` family + the `manager-factory.ts` selection** (else its minter survives). Test: no token mint site remains (grep gate).
- [ ] **Step 2: delete the token-accept paths** (with #4 live): `extract_bearer`/`authenticate_request` (`auth.rs:27/42`), `validate_token` (proxy op `helper-server.ts:~473` + `helper_client.rs:21`), `resolve_source_ip` (`helper-server.ts:433`), the `resolve({token})` subagent dispatch (replaced by Part A), and `execution_token` on the CLI wire (`request-credential.ts:~393`). `request_credential` (`request_pipeline.rs:~527`) takes #4's resolved `CallerIdentity`. **#4 owns making BOTH proxy listeners token-free** — the explicit CONNECT forward-proxy listener (`server.rs:58`) is the *primary* auth surface, not residual; #6 asserts it. Test: no token mint/accept code in either repo (grep gate).

**Cutover order (state it, gate on it):** #4 merged+verified → **Part A** serving identity by socket (token path kept dead-but-present until box-verified) → **Part B Step 1 + Part C Step 1** delete minters → **Part C Step 2** delete the dead accept code. (Note: lace's `job-manager.ts` minter is already **not** the live path under the shim — `LACE_DOCKER_BIN` routes to the plane, which mints — so deleting it is safe regardless; the dangerous deletion is the shim minter + subagent accept, correctly ordered after Part A.) A partial #6 (Part A + delete-minters without #4) **bricks egress auth** — do not reorder.

## Part D — Browser CDP: confirm loopback-only, delete any network form

**Files (sen-core-v2):** `docker/persona-containers/browser/entrypoint.sh`; `src/credential-injection/browser/chromium-cdp-transport.ts`; `proxy-helper-protocol.ts`, `runtime-registry.ts`, `helper-server.ts`, `helper-lifecycle.ts`, `lace-runtime-registration.ts`, `remote-runtime-registrar.ts`, `types.ts`.

- [ ] **Step 1: confirm loopback-only.** PRI-2002 runs `socat UNIX-LISTEN:$SEN_BROWSER_CDP_SOCKET,fork,mode=0666 → TCP:127.0.0.1:9222` (`docker/persona-containers/browser/entrypoint.sh:62`); `:9222` has **no EXPOSE / no publish** (only 6080 noVNC); `ChromiumCdpTransport` already supports `unix:` (`chromium-cdp-transport.ts:~102`). Test (box): `:9222` unreachable from the agent/helper network; the unix socket works. (The unix-socket CDP mount itself becomes a generic embedder mount via Part B Step 2 — KEEP the relay; it's the secure path.)
- [ ] **Step 2: delete the NETWORK `browser_cdp_url` form** (the unix-socket path stays). Remove `browser_cdp_url`/`browserCdpUrl` as a network URL from **ALL** sites (the rev-2 list was incomplete): `proxy-helper-protocol.ts:~105`, `runtime-registry.ts:~94`, `helper-server.ts:~168/1255`, `helper-lifecycle.ts:~499` (`createBrowserForOperation`), **`lace-runtime-registration.ts:~82-141`**, **`remote-runtime-registrar.ts:~71-72`**, **`types.ts:~63`**, and **`chromium-cdp-transport.ts`** itself. Test: no network CDP URL remains (grep gate); `type_replace` still drives CDP over the unix socket.

---

## Cross-cutting
- **Fail-closed everywhere:** no socket / no identity association / relay failure → **deny**, no fallback. Do NOT reintroduce the empty-identity literal (#4 deletes it).
- **Helper-restart rehydration (the #5 lesson):** the socket→identity map is **in-memory** (`runtime-registry.ts:73`, `clear()` `:156`). On helper-container restart, live persona sockets go dead → live agents' credential calls fail-closed until re-spawn. (b "plane re-provisions" is **infeasible** — the shim is stateless/reactive, no helper-restart trigger.) **v1 (Ada-only): accept as documented debt** — a helper restart requires affected agents to re-spawn. **Pre-prod fix:** persist the socket→identity map in the helper's private dir + rehydrate on boot. Named, not hidden.
- **Reuse-safety (the #4 lesson):** `release_capability_socket` must **succeed (drop association + unlink) before** the socket path / `(session)` is reused; the shim teardown gates reprovision on release success. **Required test:** a torn-down agent's socket path, reused for a new agent, does NOT resolve to the old identity (mirror #4's recycled-subnet test).
- **Single-file mount feasibility:** the shim mounts one socket *file* from the `sen-credential-private` volume into the persona. Confirm the shim can express a single-file bind from a named volume's host path (`-v <volume-host-path>/<file>:/run/sen-cred.sock`) and that the helper (which owns the volume) and shim agree on the path (shim computes → passes to helper). Flag in open items.
- **Admin/proxy/approval sockets unchanged** (`0o600`, on the private volume); only the subagent socket becomes per-container. `validateSocketNotInsideSubagentDir` (`helper-lifecycle.ts:467`) extends to the per-container socket dir.
- **Vault/store/audit untouched** (`vault.ts OnePasswordCliVault`, `credential-setting.ts`, `audit-log.ts`, policy YAML).

## Sequencing within #6
1. **Part A** (helper-container-only + per-container socket on the private volume + plane provisioning) — the new boundary.
2. **Part B** (full lace decoupling) — Step 1 (token) is safe once A serves identity; Steps 2-4 (browserCdp carrier, mount names, gate) are independent decoupling and can proceed in parallel.
3. **Part C** — ONLY after #4 is merged+verified; follow the cutover order.
4. **Part D** — independent; separable sub-deliverable.

## Open items / decisions to surface
- Single-file mount from the named private volume (Cross-cutting) — confirm the shim mechanism; if a named-volume single-file bind is awkward, use a host-path-backed private dir instead.
- Helper-restart rehydration — v1 documented debt vs persist now (recommend documented debt for Ada-only).
- The CDP socket as a generic mount (Part B Step 2 / D Step 1) — confirm the browser persona's CDP unix socket threads cleanly as an embedder mount once the special field is gone.
- Whether to add an explicit `credentials` field to `PersonaSpec` later vs keep "declares the `sen-cred` mount" as the gate (YAGNI now).

## Known limitations (name them)
- main-sen is semi-trusted (orchestrates spawns) but never holds secrets, never asserts identity, and now **cannot reach a credential socket** (private volume). If the **plane** is compromised, all bets are off — but it already holds docker.sock; no new exposure.
- Helper restart kills live agents' credential access until re-spawn (v1 debt; persist pre-prod).

## Self-review (rev 3)
- **BLOCKER (main-sen reachability) fixed:** per-container sockets on the broker-private volume (main-sen-invisible) + single-file mounts → the shared-container lace processes can't reach any persona's socket; identity-by-socket is now sound.
- **BLOCKER (plugin-tool incoherence) fixed:** the credential tool stays an in-image CLI (already container-scoped + decoupled); the gate is the persona's `sen-cred` mount, plane-enforced; the dead lace manifest path is dropped.
- **Decoupling is now REAL:** Part B deletes the token minter + metadata, strips the `browserCdpSocket` carrier from lace's generic layer, and generalizes the mount names → lace ends genuinely credential-agnostic (grep gates).
- Token death enumerates **all** minters incl. the **alive** spawn-broker; cutover gated; **#4-unbuilt** is an unmissable block.
- Part D = verify-and-trim with the **complete** `browser_cdp_url` site list; the unix-socket relay (the secure path) stays.
- Restart-rehydration + reuse-safety named with required tests; single-file-mount feasibility flagged; vault/audit untouched.
- Feasibility nits corrected (`TokenMinter`, `cli/helper-entrypoint.ts` exists, browser entrypoint path, spawn-broker alive).
