---
title: "11:42:06 AM - August 18, 2025"
date: 2025-08-18T18:42:06.533Z
timestamp: 1755542526533
---

## Project Notes

## ERROR-PATTERNS.md Issues

### MAJOR PROBLEMS:

1. **Lines 158, 180, 231, 499**: References to non-existent methods
   - `agent.waitForIdle()` - DOESN'T EXIST
   - `agent.waitForComplete()` - DOESN'T EXIST
   - `agent.getQueueLength()` - need to verify

2. **Line 161**: `agent.sendMessage()` - need to verify if this exists
   - Should be `agent.processUserMessage()`

3. **Lines 14-24**: Wrong event type pattern
   - Shows adding to EventType enum
   - Reality uses EVENT_TYPES array and derives union from it

4. **Line 37**: Uses `getAllEvents()` correctly (this exists)

5. **Lines 218-221**: Wrong method for adding events
   - Shows: `threadManager.addEvent(threadId, 'AGENT_MESSAGE', { content: '...' })`
   - Reality: `threadManager.addEvent({ type: 'AGENT_MESSAGE', threadId, data: { content: '...' } })`

6. **Line 356**: Wrong addEvent signature again

7. **Line 455**: References `agent.getTokenUsage()` - need to verify

### Things that look accurate:
- Database error patterns
- Provider format differences (Anthropic vs OpenAI)
- SSE formatting issues
- Tool registration pattern (though location wrong)
- Type checking patterns
