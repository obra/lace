# `lace-tui` (WIP)

Premium terminal UI for driving Ent-protocol-speaking agents over stdio (NDJSON JSON-RPC).

## Run (WIP)

From repo root:

```bash
cd packages/tui
cargo run -- --help
```

## Test

```bash
cd packages/tui
cargo test
```

