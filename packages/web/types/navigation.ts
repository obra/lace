// ABOUTME: Navigation state types for React Router state passing
// ABOUTME: Defines types for ephemeral data passed between routes

// Type for navigation state with initial message for session creation workflow
export interface SessionNavigationState {
  initialMessage?: string;
}
