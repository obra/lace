{
  "name": "@lace/ent-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Ent protocol types, schemas, and transports",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=20.18.3"
  },
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.25.69"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.4"
  }
}

