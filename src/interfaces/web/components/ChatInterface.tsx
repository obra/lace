// ABOUTME: Main chat interface component for the Lace web app
// ABOUTME: Entry point that renders the full LaceApp component

'use client';

import { LaceApp } from './LaceApp';

export default function ChatInterface() {
  return <LaceApp />;
}